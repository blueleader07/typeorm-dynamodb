export class AddOptions {
    values: any
    where: any
}

/*
await repository.update({
                type: 'ADD',
                values: {
                    totalCount: request.addToTotal,
                    processedCount: request.addToProcessed
                },
                where: {
                    id: event.id
                }
            })
 */
